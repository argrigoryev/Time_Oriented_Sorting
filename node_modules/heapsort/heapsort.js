var floor = Math.floor;

function heapsort(array) {
	var comparator = arguments.length <= 1 || arguments[1] === undefined ? ascendantComparator : arguments[1];

	var count = array.length;
	var end = count - 1;

	heapify(array, comparator);

	while (end > 0) {
		swapElems(array, end, 0);
		end = end - 1;
		siftDown(array, 0, end, comparator);
	}

	return array;
}

function ascendantComparator(a, b) {
	return a > b ? 1 : a < b ? -1 : 0;
}

function heapify(array, comparator) {
	var count = array.length;
	var start = floor((count - 2) / 2);

	while (start >= 0) {
		siftDown(array, start, count - 1, comparator);
		start = start - 1;
	}
}

function siftDown(array, start, end, comparator) {
	var root = start;

	while (root * 2 + 1 <= end) {
		var lChild = root * 2 + 1;
		var rChild = lChild + 1;
		var swap = root;

		if (comparator(array[swap], array[lChild]) < 0) {
			swap = lChild;
		}

		if (rChild <= end && comparator(array[swap], array[rChild]) < 0) {
			swap = rChild;
		}

		if (swap === root) {
			return;
		}

		swapElems(array, root, swap);
		root = swap;
	}
}

function swapElems(array, a, b) {
	var tmp = array[a];
	array[a] = array[b];
	array[b] = tmp;
}
module.exports = exports["default"];

},{}]},{},[])("/lib/index.js")
});