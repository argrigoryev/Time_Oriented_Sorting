#include <stdio.h>
#include <unistd.h>

#include "arrays.h"

int main(int argc, char const *argv[])
{
    int sorted[arr_1_len];
    for (int i = 0; i < arr_1_len; i++) {
        usleep(arr_1[i]);
    }
    return 0;
}
